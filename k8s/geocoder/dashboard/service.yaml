apiVersion: v1
kind: Service
metadata:
  name: pelias-dashboard
  namespace: geocoder
spec:
  type: ClusterIP
  selector:
    app: pelias-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3030